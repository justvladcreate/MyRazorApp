@page
@model ExampleApplication.Pages.IndexModel
@{
    ViewData["Title"] = "Главная страница";
}
<div class="mt-4">
    <h2 class="mb-4 text-center"><i class="bi bi-bar-chart-line-fill"></i> Тестовое приложения 7</h2>

    <div class="row text-center mb-5">
        <div class="col-md-12">
            <div class="card shadow-sm border-primary mt-3">
                <div class="card-body">
                    <h5 class="card-title"><i class="bi bi-clipboard-check"></i> Данные</h5>
                    <p class="display-6">@Model.MyText</p>
                </div>
            </div>
        </div>
    </div>
</div>
